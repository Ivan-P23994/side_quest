<div class="col-span-1 flex space-x-2 justify-end items-center" id="<%= dom_id(quest) %>-buttons">
  <!-- Apply Button -->
  <%= link_to apply_for_quest_quest_path(quest), class: "w-8 h-8 flex items-center justify-center rounded bg-indigo-400 hover:bg-indigo-500 text-white", title: "Apply", data: { action: "click->modal#open", turbo_stream: "" } do %>
    <!-- Paper Airplane Icon SVG -->
    <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
      <path stroke-linecap="round" stroke-linejoin="round" d="M10.5 12.75L3 21l7.5-8.25L3 3l18 9-18 9z" />
    </svg>
  <% end %>
</div>
